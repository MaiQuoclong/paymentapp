<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Paymentapp">

        <!-- Main Activity (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Create Account Activity -->
        <activity android:name=".CreateAccountActivity" />

        <!-- Bank Login Activity (mở khi login thành công) -->
        <activity android:name=".BankLoginActivity" />
        <activity android:name=".BankLoginScreenActivity" />
        <activity android:name=".BankDashboardActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".ConfirmTransferActivity" />
        <activity android:name=".SuccessActivity" />
    </application>

</manifest>